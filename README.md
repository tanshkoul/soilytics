# Soilytics
HackTable Project

## Installation
For the Tkinter version of our app, `python3` is required on your system to run. You can download all the necessary pip packages from there with `pip install -r requirements.txt`. After this runs and finishes, run our GUI program with running `python3 ./GUI.py`.


## Concept
{need to insert stuff from pitch here}